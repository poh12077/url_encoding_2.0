package com.solbox.nana;

public class Utilities {

   static public String urlEncorder(String url, String key, int timeout, int skipPath, int exceptFile) {
		try {
			
			byte[] cipherKey;
			Facilities facilities = new Facilities();
			DataSet dataSet = facilities.inputParser(url, timeout, skipPath, exceptFile);

			SHA256 sha256 = new SHA256();
			cipherKey = sha256.encrypt(key);

			AES256 aes256 = new AES256();
			String cipher_text = aes256.encrypt(dataSet.jsonString, cipherKey);

			//facilities.printResult(cipher_text, dataSet);
			String result = dataSet.skiped_path + cipher_text + dataSet.file_name;
			return result;
		} catch (Exception e) {
			e.printStackTrace();
			return "";
		}
	}
}
